import type { ControlSchema } from "@/src/animations/types";

export const controlSchema: ControlSchema = [
  {
    key: "example",
    label: "Example",
    min: 0,
    max: 1,
    step: 0.01,
    decimals: 2,
    unit: "",
    defaultValue: 0.5,
  },
];
